{
  "version": "1.0",
  "project": "vite_react_shadcn_ts",
  "description": "Project description",
  "repositories": {
    "main": {
      "path": ".",
      "defaultBranch": "main",
      "checks": [
        "npm run build",
        "npm run lint",
        "npm test"
      ]
    }
  },
  "agent": {
    "maxIterations": 50,
    "timeout": 600
  }
}
